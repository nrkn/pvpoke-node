{
  "name": "20210912-pvpoke-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clone": "git clone --depth=1 --branch=master https://github.com/pvpoke/pvpoke.git pvpoke",
    "copy": "npm run build && npm run clone && node ./dist/build/index.js",
    "build": "tsc",
    "start": "npm run build && node ./dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mojule/files": "^0.2.4",
    "jsdom": "^17.0.0",
    "jsdom-global": "^3.0.2"
  },
  "devDependencies": {
    "@types/jsdom-global": "^3.0.2",
    "@types/node": "^16.9.1"
  }
}
